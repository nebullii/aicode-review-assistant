# GitHub Service Configuration

# Server Port
PORT=3002
FRONTEND_URL=http://localhost:3001

# Webhook URL (ngrok or production URL)
WEBHOOK_URL=https://your-ngrok-url.ngrok-free.dev

# Webhook Secret (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
WEBHOOK_SECRET=your_webhook_secret_here

# Database
DATABASE_URL=postgresql://dev:devpass123@postgres:5432/code_review

# Email Notification Configuration (Optional - feature works without it)
# Sends email notifications to PR reviewers with AI analysis previews
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM_NAME=AI Code Review Assistant
DEFAULT_REVIEWER_EMAIL=fallback@example.com

# For Gmail: Use app-specific password (not regular password)
# 1. Enable 2FA: https://myaccount.google.com/security
# 2. Generate app password: https://myaccount.google.com/apppasswords
# 3. Use that 16-character password in EMAIL_PASSWORD

# Alternative Email Services:
# - SendGrid: EMAIL_SERVICE=SendGrid (requires API key)
# - AWS SES: EMAIL_SERVICE=SES (requires AWS credentials)
# - Outlook: EMAIL_SERVICE=Outlook365
# - Custom SMTP: Configure host/port separately
