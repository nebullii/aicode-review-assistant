// Default values for analysis data
const DEFAULT_SEVERITY_COUNTS = {
  critical: 0,
  high: 0,
  medium: 0,
  low: 0
};

const DEFAULT_STYLE_CATEGORIES = {
  pep8: 0,
  pylint: 0,
  naming: 0,
  complexity: 0
};

module.exports = {
  DEFAULT_SEVERITY_COUNTS,
  DEFAULT_STYLE_CATEGORIES
};
